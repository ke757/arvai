# Arvai Kernel Server Configuration
# 此文件中的配置项会在启动时自动注入到环境变量

server:
  host: "127.0.0.1"
  port: 8731
  debug: false
  cors_origins:
    - "http://localhost:5173"
    - "http://localhost:1420"   # Tauri dev
    - "chrome-extension://*"

database:
  path: "./data/arvai.db"

app:
  name: "Arvai Kernel"
  version: "0.1.0"
